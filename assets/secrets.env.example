# relentless-inception secrets — copy to ~/.claude/relentless-inception/secrets.env (chmod 600)
# Loaded by check_prereqs.sh and adversarial_review.sh; values are never printed.
# Only needed if your fusion config enables the openrouter backend.
OPENROUTER_API_KEY=
